Usage: fastify generate-plugin [opts] <file> [--] [<plugin-options>]

Generate Swagger/OpenAPI schema for a project using @fastify/swagger.

OPTS

  --yaml=true
      generate in YAML format

  --decorator <name>
      Use a custom swagger decorator name

  -o, --options
  [env: FASTIFY_OPTIONS]
      Use custom options
