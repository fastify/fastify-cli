Usage: fastify eject-ts

Turns your application into a standalone executable with a server.ts file being added.

You can set the port to listen on with the PORT environment variable (default to 3000).